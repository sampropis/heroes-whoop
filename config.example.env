# WHOOP Developer API - Local Environment Template
# Copy this file to one of the following depending on your workflow:
#   .env.dev (for npm run dev)
#   .env.local.dev (for locald profile)
#   .env.local.prod (for localp profile)
# Or simply to .env for default

# WHOOP API Configuration
# Get these from your WHOOP Developer Dashboard: https://developer.whoop.com/dashboard
WHOOP_CLIENT_ID=your_client_id_here
WHOOP_CLIENT_SECRET=your_client_secret_here
WHOOP_REDIRECT_URI=http://localhost:3000/auth/callback

# WHOOP API URLs (configurable)
# These are the standard OAuth 2.0 endpoints for user authentication
WHOOP_AUTHORIZATION_URL=https://api.prod.whoop.com/oauth/oauth2/auth
WHOOP_TOKEN_URL=https://api.prod.whoop.com/oauth/oauth2/token
WHOOP_API_BASE_URL=https://api.prod.whoop.com/developer/v2

# Application Configuration
PORT=3000
SESSION_SECRET=your_session_secret_here_change_this_in_production
NODE_ENV=development

# OAuth Scopes (space-separated, not comma-separated)
# Available scopes: read:cycles, read:recovery, read:sleep, read:workout, read:profile, read:body_measurement
# Note: 'offline' scope might not be valid - use space separation for multiple scopes
WHOOP_SCOPES=read:cycles read:recovery read:sleep read:workout read:profile read:body_measurement 